package com.example.titleapi.repository;

import com.example.titleapi.models.RanobeModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RanobeRepository extends JpaRepository<RanobeModel, Long> {
}
